<h1>{{name}}</h1>

<ul class="result-list">
    {{#each strat in strategyConnections}}
        <li>
            <a href="#" class="btn strategy">{{strat.strategy.name}}</a>
        </li>
    {{/each}}
</ul>
